# turtle_v2 项目规则 — Cline 指令

---

## 一、code-context MCP 工具使用规则

MCP 服务器 `code-context` 已安装，优先使用以下工具节省 Token：

### 核心省 Token 原则

1. **查大文件（>200行）禁止用 `read_file`**
2. **查多个符号一次完成 → `batch_read`**
3. **修改代码用两阶段提交 → `write_file_surgical`（先 dry-run 预览 diff，确认后提交）**

### 本项目的文件大小速查

| 文件 | 行数 | 最佳工具 |
|------|------|---------|
| `src/turtle_core.py` | **673** | ⚠️ 只读特定类/函数 → `read_file_surgical` |
| `strategies/turtle_trading.py` | **~530** | ⚠️ 大文件 → `read_file_surgical` |
| `src/risk_parity.py` | **~245** | ⚠️ 大文件 → `read_file_surgical` |
| `src/benchmarks.py` | **~240** | ⚠️ 大文件 → `read_file_surgical` |
| `src/data_pipeline.py` | **~300** | 大文件 → `read_file_surgical` |
| `scripts/run_backtest.py` | **~350** | ⚠️ 大文件 → `read_file_surgical` |
| `scripts/run_comparison.py` | **~280** | 大文件 → `read_file_surgical` |
| `scripts/pull_futures.py` | **~350** | ⚠️ 大文件 → `read_file_surgical` |
| 其他 `src/*.py` | <200 | 可用 `read_file`，但优先 `read_file_surgical` |
| `scripts/*.py` | 通常 <300 | `read_file` 可接受 |
| `tests/*.py` | 通常 200-350 | `read_file` 可接受 |
| `docs/*.md` | 通常 <200 | `read_file` 可接受 |

### 省 Token 操作

- 查单个函数/类 → `read_file_surgical`
- 查多个符号 → `batch_read`
- 查调用关系 + 定义 → `explain_symbol`
- 查项目架构 → `get_semantic_repo_map`
- 搜索代码模式 → `search_code_pattern`

### 安全修改代码

- 修改函数/类 → `write_file_surgical`
  - Phase 1：传入 `newContent` → 返回 diff + token
  - Phase 2：传入 `confirm=true` + `confirmationToken` → 实际写入
- 重命名符号 → `rename_symbol`
- 在函数内/前后插入代码 → `insert_symbol`
- 删除符号 → `remove_symbol`

### 自动维护

- 文件修改后 → file watcher 自动清理缓存

---

## 二、turtle_v2 项目状态速查

**说明：以下表格在推进阶段时原地更新（替换旧状态），不会逐行追加导致膨胀。** 预估保持在 130 行以内。

### 路线图

```
S0 骨架搭建     ✅   →  S1 数据管道     ✅
S2 核心移植     ✅   →  S3 策略层       ✅
S4 风险平价     ✅   →  S5 基准对比     ✅
S6 网格搜索     ✅   →  S7 压力测试     ✅
S8 综合报告     ✅   →  V5.0 期货版    ✅ (2026-06-17)
S9 Dry-Run     ⏳
```

**V5.0 变更：** 空头 3 Bug 修复 + 期货基础设施 + A 股 ETF 失效判决

### 模块依赖

```
┌─ ETF 版本 ─────────────────────────────────────┐
│ config/turtle_config.yaml          ← 所有模块读取 │
│ data/etf_daily/{code}.parquet      ← S1 数据缓存  │
│     ↑                                              │
│ src/data_pipeline.py               ← S1: Tushare   │
│     ↑                                              │
├─ 期货版本 ─────────────────────────────────────┤
│ data/futures_daily/{code}.parquet   ← V5 数据缓存  │
│     ↑                                              │
│ scripts/pull_futures.py             ← V5: Tushare   │
│     ↑                                              │
├─ 共同核心层 ────────────────────────────────────┤
│ src/turtle_core.py                 ← S2: 纯 pandas  │
│     ↑                                              │
│ src/risk_parity.py                 ← S4: 风险平价    │
│     ↑                                              │
│ strategies/turtle_trading.py       ← S3: Backtrader  │
│     ↑                                              │
│ scripts/run_backtest.py            ← S3: 回测入口    │
│   ├── (默认)   → ETF 6品种                       │
│   ├── --t0-only → 纳指+黄金                       │
│   └── --futures → 期货 12品种                     │
└──────────────────────────────────────────────────┘
```

### ETF 品种与结算

| 代码 | 名称 | 结算 | 角色 |
|:--|:--|:--:|:--|
| 510500.SH | 中证500 | T+1 | 海龟 |
| 159845.SZ | 中证1000 | T+1 | 海龟 |
| 159915.SZ | 创业板 | T+1 | 海龟 |
| 588000.SH | 科创50 | T+1 | 海龟 |
| 513100.SH | 纳指ETF | **T+0** | 海龟 |
| 518880.SH | 黄金ETF | **T+0** | 海龟 |
| 511010.SH | 国债ETF | **T+0** | 现金管理 |

### 期货品种（12 跨市场，V5.0 新增）

| 代码 | 名称 | 交易所 | 类别 |
|:--|:--|:--:|:--:|
| CU.SHF | 沪铜 | 上期所 | 金属 |
| RB.SHF | 螺纹钢 | 上期所 | 金属 |
| RU.SHF | 橡胶 | 上期所 | 能源化工 |
| M.DCE | 豆粕 | 大商所 | 农产品 |
| Y.DCE | 豆油 | 大商所 | 农产品 |
| P.DCE | 棕榈油 | 大商所 | 农产品 |
| JM.DCE | 焦煤 | 大商所 | 能源 |
| CF.ZCE | 棉花 | 郑商所 | 农产品 |
| SR.ZCE | 白糖 | 郑商所 | 农产品 |
| TA.ZCE | PTA | 郑商所 | 能源化工 |
| I.DCE | 铁矿石 | 大商所 | 黑色 |
| SC.INE | 原油 | 能源中心 | 能源 |

### 测试状态

```
tests/test_data_pipeline.py     20 passed  ✅
tests/test_turtle_core.py       52 passed  ✅
tests/test_turtle_strategy.py   22 passed  ✅
tests/test_risk_parity.py       22 passed  ✅
tests/test_benchmarks.py        10 passed  ✅
tests/test_gen_report.py        10 passed  ✅
tests/test_grid_search.py       14 passed  ✅
tests/test_stress_test.py       32 passed  ✅
─────────────────────────────────────────
合计                           185 passed  ✅
```

---

## 三、开发规范

### 版本控制

- 完成一个阶段后必须 `git add -A && git commit -m "消息"`
- `git commit -m` 消息体**禁止包含换行**、`task_progress:`、checklist（违反会触发 PowerShell 解析错误）
- 提交消息格式：`S{N}: 阶段名 — 简要说明`

### 测试

- 每个阶段必须包含对应的 `tests/test_*.py`
- 提交前必须跑 `py -m pytest tests/ -q` 确认全部通过

### 数据

- 未设置 `TUSHARE_TOKEN` 环境变量时，数据管道会抛出 `ValueError`
- ETF 数据缓存：`data/etf_daily/{code}.parquet`（通过 `py scripts/pull_data.py`）
- 期货数据缓存：`data/futures_daily/{code}.parquet`（通过 `py scripts/pull_futures.py`）
- 回测三种模式：
  - `py scripts/run_backtest.py` — ETF 全品种
  - `py scripts/run_backtest.py --t0-only` — 仅 T+0（纳指+黄金）做空验证
  - `py scripts/run_backtest.py --futures` — 期货 12 品种双向
- 期货首次拉取全部 12 品种需 ~5 分钟（API 限速），后续增量秒级
- 郑商所主连续列使用 `.ZCE` 后缀（非 `.CZC`），示例：`CF.ZCE`
