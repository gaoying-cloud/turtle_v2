验证计划分 4 个维度：

| # | 维度 | 检查内容 |
|---|------|---------|
| 1 | **语法与导入** | `AST` 解析语法正确性；新函数可导入 |
| 2 | **函数逻辑** | `should_activate_trailing_stop()` 边界条件；`_update_trailing_stop()` 只上移保护；`_should_exit()` 三重退出检查顺序 |
| 3 | **调用链完整性** | `next()` → `_update_trailing_stop()` → `_should_exit()` → `_execute_exit()` 路径不遗漏 |
| 4 | **经典海龟交易法则的 A 股 ETF 趋势跟踪系统(项目在：C:\py_学习\automated_trading)** | 对比 `automated_trading/src/strategy_engine.py` 的 `_check_exits()`，确认退出逻辑等价 |

切换到 **ACT MODE** 我来执行。